import type { ReactNode } from "react";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
export function DataTable<T>({ columns, rows, render, empty = "No records found." }: { columns: string[]; rows: T[]; render: (row: T) => ReactNode[]; empty?: string }) { return <div className="rounded-[10px] bg-white shadow-1 dark:bg-gray-dark dark:shadow-card"><Table><TableHeader><TableRow className="border-none bg-[#F7F9FC] dark:bg-dark-2">{columns.map(c => <TableHead key={c} className="whitespace-nowrap font-medium text-dark dark:text-white">{c}</TableHead>)}</TableRow></TableHeader><TableBody>{rows.length ? rows.map((row, i) => <TableRow key={i}>{render(row).map((cell, j) => <TableCell key={j}>{cell}</TableCell>)}</TableRow>) : <TableRow><TableCell colSpan={columns.length} className="py-10 text-center">{empty}</TableCell></TableRow>}</TableBody></Table></div>; }
