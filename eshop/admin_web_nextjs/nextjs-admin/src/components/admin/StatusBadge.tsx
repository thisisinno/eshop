import { cn } from "@/lib/utils";
export function StatusBadge({ value }: { value: string | boolean }) { const label = typeof value === "boolean" ? (value ? "Active" : "Inactive") : value.replaceAll("_", " "); const positive = ["approved", "active", "verified", "true"].includes(String(value)); const warning = ["pending", "draft", "expired"].includes(String(value)); return <span className={cn("rounded-full px-3 py-1 text-xs font-medium capitalize", positive && "bg-[#219653]/8 text-[#219653]", warning && "bg-[#FFA70B]/8 text-[#FFA70B]", !positive && !warning && "bg-[#D34053]/8 text-[#D34053]")}>{label}</span>; }
